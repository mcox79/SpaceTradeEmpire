{
    "queue_contract_version": "2.2",
    "queue_ordering": "MULTIKEY_V1",
    "generated_utc": "2026-03-04T14:30:00Z",
    "queue_intent": "20 tranche-2 gates (First Light 13 closed out); HEAD=1add65c",
    "tasks": [
        {
            "gate_id": "GATE.S1.AUDIO.SFX_CORE.001",
            "task_id": "GATE.S1.AUDIO.SFX_CORE.001.T134",
            "status": "TODO",
            "title": "GATE.S1.AUDIO.SFX_CORE.001: Thrust + turret + hit + explosion SFX",
            "intent": "AudioStreamPlayer3D nodes: engine thrust (loop), turret fire (one-shot), bullet hit (one-shot), explosion (one-shot). Placeholder synth .wav assets. No SimCore.",
            "evidence_paths": ["scripts/core/game_manager.gd", "scripts/bullet.gd", "scenes/player.tscn", "scenes/bullet.tscn"],
            "constraints": [
                "Single-session scope: stay within evidence and expected touch paths.",
                "If evidence is missing or tests fail, escalate via DEFAULT."
            ],
            "completion_hint": ["godot --headless --path . -s res://scripts/tests/test_audio_v0.gd"],
            "task_preflight": ["anchor=scripts/core/game_manager.gd"],
            "escalation_rules": [
                { "when": "DEFAULT", "route": "STOP", "note": "Escalate if blocked or scope expands." }
            ],
            "parallel_group": "bridge",
            "tier": 1,
            "blocks": [],
            "verify": ["dotnet build \"Space Trade Empire.csproj\" --nologo -v q"]
        },
        {
            "gate_id": "GATE.S1.AUDIO.AMBIENT.001",
            "task_id": "GATE.S1.AUDIO.AMBIENT.001.T268",
            "status": "TODO",
            "title": "GATE.S1.AUDIO.AMBIENT.001: Space ambient + warp + dock chimes",
            "intent": "AudioStreamPlayer ambient drone (loop), warp whoosh (one-shot on lane jump), dock chime (one-shot on station dock). Volume ducking during combat. No SimCore.",
            "evidence_paths": ["scripts/core/game_manager.gd", "scenes/Playable_Prototype.tscn"],
            "constraints": [
                "Single-session scope: stay within evidence and expected touch paths.",
                "If evidence is missing or tests fail, escalate via DEFAULT."
            ],
            "completion_hint": ["godot --headless --path . -s res://scripts/tests/test_audio_v0.gd"],
            "task_preflight": ["anchor=scripts/core/game_manager.gd"],
            "escalation_rules": [
                { "when": "DEFAULT", "route": "STOP", "note": "Escalate if blocked or scope expands." }
            ],
            "parallel_group": "bridge",
            "tier": 2,
            "blocks": ["GATE.S1.AUDIO.SFX_CORE.001"],
            "verify": ["dotnet build \"Space Trade Empire.csproj\" --nologo -v q"]
        },
        {
            "gate_id": "GATE.S1.SAVE_UI.PAUSE_MENU.001",
            "task_id": "GATE.S1.SAVE_UI.PAUSE_MENU.001.T456",
            "status": "TODO",
            "title": "GATE.S1.SAVE_UI.PAUSE_MENU.001: Escape key pause menu (save/load/quit)",
            "intent": "Escape toggles pause overlay (Control). Buttons: Resume, Save, Load, Quit. get_tree().paused = true. No SimCore.",
            "evidence_paths": ["scripts/core/game_manager.gd", "scripts/ui/hud.gd", "scenes/ui_hud.tscn"],
            "constraints": [
                "Single-session scope: stay within evidence and expected touch paths.",
                "If evidence is missing or tests fail, escalate via DEFAULT."
            ],
            "completion_hint": ["godot --headless --path . -s res://scripts/tests/test_pause_menu_v0.gd"],
            "task_preflight": ["anchor=scripts/core/game_manager.gd"],
            "escalation_rules": [
                { "when": "DEFAULT", "route": "STOP", "note": "Escalate if blocked or scope expands." }
            ],
            "parallel_group": "bridge",
            "tier": 1,
            "blocks": [],
            "verify": ["dotnet build \"Space Trade Empire.csproj\" --nologo -v q"]
        },
        {
            "gate_id": "GATE.S1.SAVE_UI.SLOTS.001",
            "task_id": "GATE.S1.SAVE_UI.SLOTS.001.T589",
            "status": "TODO",
            "title": "GATE.S1.SAVE_UI.SLOTS.001: 3 save slots with metadata display",
            "intent": "Save/Load sub-panel: 3 slots with timestamp, credits, system name. Wires existing SimBridge save/load. No SimCore changes.",
            "evidence_paths": ["scripts/bridge/SimBridge.cs", "scripts/ui/hud.gd"],
            "constraints": [
                "Single-session scope: stay within evidence and expected touch paths.",
                "If evidence is missing or tests fail, escalate via DEFAULT."
            ],
            "completion_hint": ["godot --headless --path . -s res://scripts/tests/test_save_slots_v0.gd"],
            "task_preflight": ["anchor=scripts/ui/hud.gd"],
            "escalation_rules": [
                { "when": "DEFAULT", "route": "STOP", "note": "Escalate if blocked or scope expands." }
            ],
            "parallel_group": "bridge",
            "tier": 2,
            "blocks": ["GATE.S1.SAVE_UI.PAUSE_MENU.001"],
            "verify": ["dotnet build \"Space Trade Empire.csproj\" --nologo -v q"]
        },
        {
            "gate_id": "GATE.S1.DISCOVERY_INTERACT.SCAN.001",
            "task_id": "GATE.S1.DISCOVERY_INTERACT.SCAN.001.T712",
            "status": "TODO",
            "title": "GATE.S1.DISCOVERY_INTERACT.SCAN.001: Scan/Analyze buttons wired to SimBridge",
            "intent": "Add Scan + Analyze buttons to discovery panel. Scan calls AdvanceDiscoveryPhaseV0. Button states reflect phase.",
            "evidence_paths": ["scripts/ui/DiscoverySitePanel.gd", "scripts/bridge/SimBridge.cs", "scripts/core/game_manager.gd"],
            "constraints": [
                "Single-session scope: stay within evidence and expected touch paths.",
                "If evidence is missing or tests fail, escalate via DEFAULT."
            ],
            "completion_hint": ["godot --headless --path . -s res://scripts/tests/test_station_dock_v0.gd"],
            "task_preflight": ["anchor=scripts/ui/DiscoverySitePanel.gd"],
            "escalation_rules": [
                { "when": "DEFAULT", "route": "STOP", "note": "Escalate if blocked or scope expands." }
            ],
            "parallel_group": "bridge",
            "tier": 1,
            "blocks": [],
            "verify": ["dotnet build \"Space Trade Empire.csproj\" --nologo -v q"]
        },
        {
            "gate_id": "GATE.S1.DISCOVERY_INTERACT.RESULTS.001",
            "task_id": "GATE.S1.DISCOVERY_INTERACT.RESULTS.001.T843",
            "status": "TODO",
            "title": "GATE.S1.DISCOVERY_INTERACT.RESULTS.001: Scan results display (unlock type/desc)",
            "intent": "After scan, show results: unlock type, description, awarded items. Reads GetDiscoverySnapshotV0. Display-only.",
            "evidence_paths": ["scripts/ui/DiscoverySitePanel.gd", "scripts/bridge/SimBridge.cs"],
            "constraints": [
                "Single-session scope: stay within evidence and expected touch paths.",
                "If evidence is missing or tests fail, escalate via DEFAULT."
            ],
            "completion_hint": ["godot --headless --path . -s res://scripts/tests/test_station_dock_v0.gd"],
            "task_preflight": ["anchor=scripts/ui/DiscoverySitePanel.gd"],
            "escalation_rules": [
                { "when": "DEFAULT", "route": "STOP", "note": "Escalate if blocked or scope expands." }
            ],
            "parallel_group": "bridge",
            "tier": 2,
            "blocks": ["GATE.S1.DISCOVERY_INTERACT.SCAN.001"],
            "verify": ["dotnet build \"Space Trade Empire.csproj\" --nologo -v q"]
        }
    ]
}
